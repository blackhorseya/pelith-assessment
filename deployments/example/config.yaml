logger:
  level: debug
  format: console

services:
  server:
    http:
      port: 8080

    grpc:
      port: 50051

    storage:
      dsn: "postgres://root:changeme@localhost:5432/pelith?sslmode=disable"
